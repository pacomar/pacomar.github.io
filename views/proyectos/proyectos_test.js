'use strict';

describe('myApp.proyectos module', function() {

  beforeEach(module('myApp.proyectos'));

  describe('proyectos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var proyectosCtrl = $controller('ProyectosCtrl');
      expect(proyectosCtrl).toBeDefined();
    }));

  });
});