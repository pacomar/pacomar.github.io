'use strict';

describe('myApp.conocimientos module', function() {

  beforeEach(module('myApp.conocimientos'));

  describe('conocimientos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var conocimientosCtrl = $controller('ConocimientosCtrl');
      expect(conocimientosCtrl).toBeDefined();
    }));

  });
});