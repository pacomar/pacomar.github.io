'use strict';

describe('myApp.contacto module', function() {

  beforeEach(module('myApp.contacto'));

  describe('contacto controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});